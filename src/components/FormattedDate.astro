---
export interface Props {
	date: Date;
	class: string;
}

const { date } = Astro.props;
---

<time {...Astro.props} datetime={date.toISOString()}>
	{
		date.toLocaleDateString("en-us", {
			year: "numeric",
			month: "short",
			day: "numeric",
		})
	}
</time>
